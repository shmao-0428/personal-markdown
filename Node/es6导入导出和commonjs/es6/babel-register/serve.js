// const Koa = require('koa');
// const app = new Koa();

// app.listen(8225, console.log('application is start at port 8225'));

import Koa from 'koa';
const app = new Koa();

app.listen(8225, console.log('application is start at port 8225'));
